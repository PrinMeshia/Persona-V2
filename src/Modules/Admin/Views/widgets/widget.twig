<div class="col-sm-4">
    <div class="card">
        <div class="card-block">
        <h4 class="card-title">
            {{count}}
        </h4>
        <p class="card-text">
            Arcticle{%if count > 1%}s{% endif %}
        </p>
        <a href="{{path('admin.posts')}}" class="btn btn-primary">Gérer les articles</a>
        </div>
    </div>
</div>